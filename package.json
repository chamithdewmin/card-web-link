{
  "name": "card-web-link",
  "private": true,
  "description": "Vite app lives in frontend/. Run npm install inside frontend/",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
